[project]
name = "agentforge"
version = "0.1.0"
description = "Modulares Framework für KI-Agenten, Konnektoren, Datenaufbereitung und Monitoring"
requires-python = ">=3.10"

[project.scripts]
agentforge = "agentforge.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
